───────────────────────────────────────────────────────────────────────────────
                           🏬 SHOP MODULE ARCHITECTURE
───────────────────────────────────────────────────────────────────────────────
FRONTEND (Vue 3)                             BACKEND (Flask)                     DATABASE (SQLAlchemy / SQLite)
───────────────────────────────────────────────────────────────────────────────

1️⃣ ShopDashboard.vue ─────────────────────▶  GET /api/v1/shop/dashboard  ─────▶  ProductCatalog, Inventory
   │                                         (shop_routes.py)
   │
   │  Displays KPIs, charts, and summary data
   │  - Fetches total sales, stock levels
   │  - API: api.get('/shop/dashboard')
   │
   ▼
   Axios (src/api/apiShop.js)
───────────────────────────────────────────────────────────────────────────────

2️⃣ ShopInquiry.vue ───────────────────────▶  POST /api/v1/inquiry/create  ─────▶  Inquiry Table
   │                                         (inquiry_routes.py)
   │
   │  Form submission: Customer sends inquiry
   │  - API: api.post('/inquiry/create', formData)
   │
   ▼
   Backend validates → saves to DB
   - Returns success message JSON
───────────────────────────────────────────────────────────────────────────────

3️⃣ ShopInventory.vue ────────────────────▶  GET /api/v1/inventory/all     ─────▶  Inventory Table
   │                                         (inventory_routes.py)
   │
   │  Displays product stock
   │  - API: api.get('/inventory/all')
   │  - Add new item: api.post('/inventory/add')
   │  - Update: api.put('/inventory/update/<id>')
   │  - Delete: api.delete('/inventory/delete/<id>')
   │
   ▼
   CRUD operations via SQLAlchemy
───────────────────────────────────────────────────────────────────────────────

4️⃣ ShopMarketing.vue ────────────────────▶  GET /api/v1/marketing/data    ─────▶  Marketing Table
   │                                         (marketing_routes.py)
   │
   │  Fetches campaign performance data
   │  - API: api.get('/marketing/data')
   │  - Add new campaign: api.post('/marketing/add')
   │
   ▼
   Data returned as JSON → shown in charts
───────────────────────────────────────────────────────────────────────────────

FRONTEND FLOW:
───────────────────────────────────────────────────────────────────────────────
User clicks button → Axios → API endpoint (Flask Route)
→ Controller executes business logic → interacts with Model (DB)
→ Flask returns JSON → Axios updates Vue component (re-renders UI)

───────────────────────────────────────────────────────────────────────────────
MODULE RELATIONSHIPS (Simplified UML)
───────────────────────────────────────────────────────────────────────────────

ShopDashboard.vue
    │
    ├── calls → inventory_routes.py (for stock)
    ├── calls → sales_service.py (for analytics)
    └── calls → product_routes.py (for product list)

ShopInquiry.vue
    └── calls → inquiry_routes.py
          └── interacts with Inquiry model

ShopInventory.vue
    └── calls → inventory_routes.py
          └── interacts with Inventory model

ShopMarketing.vue
    └── calls → marketing_routes.py
          └── interacts with Marketing model
───────────────────────────────────────────────────────────────────────────────

📡 DATA ROUNDTRIP (Example for ShopInventory)
───────────────────────────────────────────────────────────────────────────────
[Vue Button: "Add Product"]
       │
       ▼
Axios → POST /inventory/add
       │
       ▼
Flask Route (inventory_routes.py)
       │
       ▼
SQLAlchemy Model → db.session.add(new_item)
       │
       ▼
DB Commit → Success → JSON Response
       │
       ▼
Frontend updates Inventory Table in UI
───────────────────────────────────────────────────────────────────────────────
